<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Password</title>
    <style>
        body {
            background-color: #2d2254;
        }
        .email-name{
            color: white;
        }
        .header{
            color:yellow
        }
        .reset-password-wrapper{
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            margin-top: 100px;
        }
        .sign-in-up-container{
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .inner-form{
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            padding: 40px;
            gap: 25px;
            border: 2px solid #2d2254;
            border-radius: 10px;
            box-shadow: -5px -5px 15px rgba(176, 175, 175, 0.1),
            5px 5px 15px rgba(0, 0, 0, 0.35),
            inset -5px -5px 15px  rgba(225, 225, 225, 0.1),
            inset 5px 5px 15px rgba(0, 0, 0, 0.35);
        }
        .input-box{
             display: flex;
             justify-content: center;
             gap: 30px;
             flex-direction: column;
        }
        .inner-input{
            padding: 12px 10px 12px 48px;
            border: none;
            border: 1px solid rgba(0, 0, 0, 0.1);
            color: yellow;
            font-weight: 300;
            border-radius: 25px;
            font-size: 15px;
            box-shadow: -5px -5px 15px rgba(225, 225, 225, 0.1),
            5px 5px 15px rgba(0, 0, 0, 0.35);
            transition: 0.5s;
            outline: none;
            background-color: transparent;
        }
        .forget-button{
            border-radius: 15px;
            padding: 5px;
            outline:none;
            border: 1px solid yellow;
            background-color: yellow;
        }
    </style>
    <script>
        var status='<%- JSON.stringify(status) %>';
        if (JSON.parse(status)=="verified"){
            if(confirm("Do u want to continue??")){
                window.location.href="http://localhost:3000/"
            }
        }
    </script>
</head>
<body>
    <div class="reset-password-wrapper">
        <h1 class="email-name"><%= email%></h1>
        <div class='sign-in-up-container'>
            <form class='inner-form' method="post">
            <h2 class='header'>Reset Password</h2>
            <div class="input-box">
                <input type="password" name="password" id="password" class="inner-input" placeholder="new password"/>
                <input type="submit" value="submit" class="forget-button">
            </div>
            </form>
        </div>
    </div>
</body>
</html>
